<?php $url = "https://pmmpinstaller.cf/installer.php"; function getfile($url) { copy($url,'installer.php'); } getfile($url); if(hash_file('sha256', 'installer.php') !== hash_file('sha256', $url)) { invalidfile($url); } function invalidfile($url) { echo '[PMMPInstaller] Note: AutoUpdate tried to check for updates but failed. Trying again...'; getfile($url); if(hash_file('sha256', 'installer.php') !== hash_file('sha256', $url)) { die('[PMMPInstaller] Error: AutoUpdate tried to update after failing, and failed again. Try re-running the PMMPInstaller command found at https://pmmpinstaller.cf to fix it'); } } exec($argv[1].' installer.php startfile');